B = array(double, [500, 500, 500])
C = array(double, [500, 500])
D = array(double, [500, 500])

tmp = vcontract(B, D, [[3, 1]])
A = contract(tmp, C, [[2, 1]])

l = build(A)

l1 = parallelize(l, 1, None)
l2 = interchange(l1, [[2, 3]])

codegen([l, l2])
